apiVersion: v1
kind: Service
metadata:
  annotations:
    kompose.cmd: kompose convert
    kompose.version: 1.20.0 ()
  creationTimestamp: null

  name: {{ .Release.Name }}
  namespace: {{ .Values.namespace }}
spec:
  ports:
  - name: "9000"
    port: 27017
    targetPort: 27017
  selector:
    name: {{ .Release.Name }}
status:
  loadBalancer: {}
