apiVersion: apps/v1
kind: Deployment
metadata:
  annotations:
    kompose.cmd: kompose convert -f config/client.yaml
    kompose.version: 1.22.0 (955b78124)
  creationTimestamp: null
  labels:
    io.kompose.service: client
  name: client
spec:
  replicas: 1
  selector:
    matchLabels:
      io.kompose.service: client
  strategy:
    type: Recreate
  template:
    metadata:
      annotations:
        kompose.cmd: kompose convert -f config/client.yaml
        kompose.version: 1.22.0 (955b78124)
      creationTimestamp: null
      labels:
        io.kompose.service: client
    spec:
      containers:
        - args:
            - /bin/bash
            - -c
            - sleep 10 && fedn run client -in ./config/settings-client.yaml
          env:
            - name: GET_HOSTS_FROM
              value: dns
          image: scaleoutsystems/fedn-client:master
          name: client
          resources: {}
          volumeMounts:
            - mountPath: /app/config
              name: client-claim0
            - mountPath: /app/data
              name: client-claim1
          workingDir: /app/
      restartPolicy: Always
      volumes:
        - name: client-claim0
          persistentVolumeClaim:
            claimName: client-claim0
        - name: client-claim1
          persistentVolumeClaim:
            claimName: client-claim1
status: {}
